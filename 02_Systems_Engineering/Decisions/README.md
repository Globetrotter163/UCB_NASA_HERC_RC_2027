# Objetos de decisión

Una nota por decisión `DEC-*`, creada desde `Template_Decision.md`.

`Decision_Log.md` es el índice humano oficial. Estas notas aportan frontmatter consultable; ningún agente puede cambiar `status` a `approved`.
